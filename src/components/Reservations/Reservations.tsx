import * as React from "react";
import Greeting from "../@common/Greeting/Greeting";

const Reservations: React.FC = () => {
  return (
    <div>
      <Greeting greetingMessage="Hello from Reservations" />
    </div>
  );
};

export default Reservations;
